#include "monty.h"

/**
 * switch_to_stack - switches the queue to a stack.
 * @head: stack head
 * @counting: line_number
 * Return: no return
 */
void switch_to_stack(stack_t **head, unsigned int counting)
{
	(void)head;
	(void)counting;
	bus.lifi = 0;
}

